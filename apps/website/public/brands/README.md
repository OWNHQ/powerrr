# Brand asset sources

- `own.svg`: official OWN white-mode wordmark from
  [own.casa](https://own.casa/own-logo.svg).
- Token SVGs in `../tokens/`: downloaded from the official Morpho metadata CDN
  URLs published by `morpho-org/morpho-blue-api-metadata`.

These are source assets, not recreated or traced logos.
